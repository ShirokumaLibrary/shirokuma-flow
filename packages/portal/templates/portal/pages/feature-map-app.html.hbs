<div class="max-w-5xl">
  <div class="mb-6">
    <a href="/apps/{{urlEncode appId}}" class="text-sm text-indigo-600 hover:underline">&larr; {{app.name}}</a>
    <h1 class="text-2xl font-bold text-gray-900 mt-2">{{app.name}} - 機能マップ</h1>
    <p class="text-gray-500 text-sm mt-1">{{totalModules}} モジュール</p>
  </div>

  <div class="space-y-4">
    {{#each modules}}
    <details class="bg-white rounded-lg border border-gray-200 group">
      <summary class="flex items-center justify-between p-4 cursor-pointer list-none hover:bg-gray-50 rounded-lg">
        <span class="font-medium text-gray-900">{{this.name}}</span>
        <div class="flex gap-2 text-xs text-gray-500">
          {{#if this.screenCount}}
          <span class="bg-blue-50 text-blue-700 px-2 py-0.5 rounded">画面 {{this.screenCount}}</span>
          {{/if}}
          {{#if this.componentCount}}
          <span class="bg-green-50 text-green-700 px-2 py-0.5 rounded">コンポーネント {{this.componentCount}}</span>
          {{/if}}
          {{#if this.actionCount}}
          <span class="bg-amber-50 text-amber-700 px-2 py-0.5 rounded">アクション {{this.actionCount}}</span>
          {{/if}}
        </div>
      </summary>
      <div class="px-4 pb-4 border-t border-gray-100 pt-3">
        {{#ifNotEmpty this.screens}}
        <div class="mb-2">
          <h3 class="text-xs font-semibold text-blue-700 mb-1">画面</h3>
          <ul class="space-y-0.5">
            {{#each this.screens}}
            <li class="text-sm text-gray-700">{{this.name}}</li>
            {{/each}}
          </ul>
        </div>
        {{/ifNotEmpty}}
      </div>
    </details>
    {{/each}}

    {{#unless modules}}
    <div class="text-center py-12 text-gray-400">このアプリに機能マップがありません</div>
    {{/unless}}
  </div>
</div>
