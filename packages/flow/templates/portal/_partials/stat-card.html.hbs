<div class="bg-white rounded-lg border border-gray-200 p-4">
  <div class="text-2xl font-bold text-gray-900">{{value}}{{unit}}</div>
  <div class="text-sm text-gray-500 mt-1">{{label}}</div>
  {{#if href}}
  <a href="{{href}}" class="text-xs text-indigo-600 hover:underline mt-2 block">詳細を見る →</a>
  {{/if}}
</div>
