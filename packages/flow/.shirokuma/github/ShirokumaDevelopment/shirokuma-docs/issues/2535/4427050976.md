## 実装完了

ADR-v3-022 第二改訂版に基づく **プラグイン (Review 削除/approve 意味変更) + ドキュメント整合 + GitHub Projects 設定 + ADR 昇格** を完了しました。

**PR:** #2540 (base: `epic/2529-6-status-model`)
**ブランチ:** `feat/2535-plugin-docs-projects`
**ステータス:** Review

### コミット履歴

| Hash | 内容 |
|------|------|
| `14a12510` | feat: プラグイン整合 + docs 更新 + Projects 設定 + ADR 昇格 (sub5) |
| `2e423e0` | docs: PR #2540 simplify レビュー指摘の修正 - 旧 Review→ToDo 記述消去 |

### 達成した受け入れ基準

- [x] **AC1**: ADR-v3-022 (#2527) を `Accepted` に昇格
- [x] **AC10**: プラグイン 6 ファイル (JA/EN 各) が ADR-v3-022 第二改訂版と一致
- [x] **AC11**: `project setup` の Status フィールド既定値が 6 値（Backlog: GRAY 追加）
- [x] **AC12**: docs (`docs/`, `.shirokuma/rules/`, `CLAUDE.md`) が 6 値・4 系統で統一

### 変更ファイル

#### A. プラグイン (JA / EN 各 6 ファイル)
- `prepare-flow / design-flow`: 親 Issue Review 遷移削除、計画/設計 Issue 子の Backlog → Review に変更
- `implement-flow`: `approve` の意味変更（Review → Done）反映
- `review-flow`: コード修正時 Review → In progress に `--rollback` フラグ必須注記
- `analyze-issue`: plan ロールが計画 Issue 子にコメント投稿
- `create-item-flow`: 初期 Status = Backlog 対応

#### B. ドキュメント
- `docs/specs/workflow/status.md`: 6 値 + 4 系統遷移テーブル、Backlog → ToDo を `syncParentStatus` 表記に修正
- `docs/guide/workflows/lifecycle-overview.md`: 6 値・4 系統対応、旧 `Review → ToDo` 記述全消去、Mermaid 重複削除
- `.shirokuma/rules/shirokuma-flow/github-commands.md`: approve = Review → Done 修正、`--rollback` 例追加
- `.shirokuma/rules/shirokuma-flow/cli-status-update-policy.md`: itemType 必須化追記
- `.shirokuma/rules/shirokuma-flow/adr-index.md`: ADR-v3-022 → Accepted

#### C. GitHub Projects 設定
- `packages/flow/src/commands/project/setup.ts`: Status フィールド既定値を 6 値（Backlog: GRAY 追加）

#### D. ADR 昇格
- Discussion #2527 (ADR-v3-022 第二改訂版) を `Status: Proposed → Accepted` に昇格

### simplify レビュー対応

PR #2540 作成後の単一エージェントレビュー（コード再利用 + 品質統合）で HIGH/MED 指摘を即時対応:

- **HIGH**: `github-commands.md` approve 説明 stale → ADR-v3-022 準拠に修正
- **HIGH**: `lifecycle-overview.md` 半マイグレーション解消（旧 Review → ToDo パス、親 Issue が Review に上がる記述を全消去）
- **MED**: Mermaid 重複 edge 削除
- **MED**: `status.md:26` Backlog → ToDo の command 表記を `syncParentStatus` に修正

### セキュリティレビュー

`/security-review` 実行: **No HIGH or MEDIUM security findings.**

YAML 解析は `yaml.parse` （safe loader）、GraphQL は parameterized variables、`reject.ts` の tmp file は `isIssueNumber` で検証された数字のみ含む。新たな脆弱性なし。

### テスト結果

- ビルド・型チェック: 通過
- テスト: 全件通過

### 後続作業

- sub6 (#2536, L): `migrate-v3-018.ts` 削除 + `--rollback` 破壊的変更リリースノート

これで sub5 完了。マージ後は **sub6 のみが残作業** となり、その完了で ADR-v3-022 実装エピック (#2529) が closeable な状態に。
