## 実装完了

ADR-v3-022 第二改訂版に基づく **テスト全面更新** を完了しました。

**PR:** #2542 (base: `epic/2529-6-status-model`)
**ブランチ:** `test/2534-6values-tests`
**ステータス:** Review

### コミット履歴

| Hash | 内容 |
|------|------|
| `9df50ba7` | test: 4テーブル網羅 + .legacy 6値版復活 + Backlog起点 E2E (sub4) |
| (lint 修正コミット 2 件) | testdoc-japanese / duplicate-testdoc 修正 |
| `097443fb` | test: PR #2542 simplify レビュー指摘の修正 - reject mock + .legacy 削除 |

### 達成した受け入れ基準 (AC13)

- [x] `.legacy.ts.disabled` ファイル群が 6 値版に書き換えられて `.test.ts` として復活
- [x] 4 テーブル × 6 値 × Forward/Rollback の網羅テストが追加
- [x] Backlog 起点 lifecycle E2E テストが追加
- [x] `add.legacy.ts.disabled` の status 非依存 28 テストが復活（sub3 PR レビュー指摘の最優先タスク）
- [x] 全テスト通過 (166 suites / 2339 tests)
- [x] ビルド・型チェック通過

### 追加されたテストファイル

| ファイル | 内容 |
|---------|------|
| `__tests__/utils/status-workflow.test.ts` (959 行) | 4 テーブル × 6 値 + getAllowedTransitions / validateTransition 網羅 |
| `__tests__/commands/issue/add.test.ts` (1324 行) | status 非依存 28 テスト復活 |
| `__tests__/commands/reject.test.ts` (217 行 → +63 行) | 6 値版（--rollback 必須化 + itemType 別ルーティング + Review-only ガード） |
| `__tests__/commands/status/approve.test.ts` (624 行) | 6 値版（Review→Done + 親同期） |
| `__tests__/integration/workflow/backlog-startup-lifecycle.test.ts` (484 行) | **新規 E2E**: Backlog 起点 lifecycle 全体フロー |
| `__tests__/integration/workflow/prepare-to-implement.test.ts` (396 行) | 6 値版 integration |
| `__tests__/integration/workflow/reject-cycle.test.ts` (329 行) | 6 値版 integration |

### 削除した dead code

- `__tests__/commands/reject.legacy.ts.disabled` (171 行)
- `__tests__/utils/status-workflow.legacy.ts.disabled` (824 行)

合計 997 行削除（CLAUDE.md 「互換スタブ・段階的 deprecation は不要」ポリシー準拠）。

### simplify レビュー対応

PR #2542 作成後の simplify レビューで HIGH/LOW 級指摘を即時対応:

- **HIGH (BUG-1)**: `reject.test.ts` が `resolveCurrentStatus` をモックせず実 API を呼んでいた問題を修正。`jest.unstable_mockModule` 追加 + itemType 別ルーティングの 3 テスト追加（core routing を unit test レベルでカバー）
- **LOW (STALE-1)**: `.legacy.ts.disabled` 残存ファイル 2 件を削除

未対応（フォローアップ候補として記録）:
- **MEDIUM (DUP-1)**: 7 integration workflow テストのヘルパー重複（~1500 行）。共通 fixture (`workflow-test-fixture.ts`) への抽出は別 PR でリファクタすべき
- **LOW (DUP-3)**: `reject.test.ts` と `reject-cycle.test.ts` の validation テスト重複
- **LOW (NAME-1)**: `describe` prefix の `"結合: "` 統一

### セキュリティレビュー

`/security-review` 実行: **No vulnerabilities identified.**

新規テストは pure データ/validation logic で、新たな I/O・exec・eval・deserialization・auth/crypto logic・hardcoded secrets・sensitive-data logging を導入していない。

### テスト結果

166 suites / 2339 tests passed (2 skipped)。前回（sub6 後）159/2180 から **+7 スイート / +159 テスト追加**。リグレッションなし。

### エピック #2529 完了状況

| Sub | PR | 状態 |
|-----|----|------|
| sub1 (#2531) | #2537 | マージ済み |
| sub2 (#2532) | #2538 | マージ済み |
| sub3 (#2533) | #2539 | マージ済み |
| sub5 (#2535) | #2540 | マージ済み |
| sub6 (#2536) | #2541 | マージ済み |
| **sub4 (本 Issue)** | **#2542** | **Review** |

**これで ADR-v3-022 実装エピックの全サブ Issue が完成**。本 PR マージで `pr merge` の自動アクション「全実作業サブ Issue 完了 + 全計画完了」を検知し、`integration → develop` の最終 PR を自動作成 + `/review-flow` を提案します。

最終 PR マージで:
- 課題 Issue #2529 を Done 化
- 計画 Issue #2530 を Done 化
- ADR-v3-022 第二改訂版実装の epic 完了
