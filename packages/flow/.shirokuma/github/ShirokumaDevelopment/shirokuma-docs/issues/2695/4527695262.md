## コミット完了

finalize-worker のセキュリティレビューで検出した dead code を修正した。`message.includes("422")` による文字列マッチを `e instanceof RequestError && e.status === 422` の型安全な status チェックに置き換え、テストも本物の RequestError を使うよう更新した。

**ブランチ:** fix/2695-issue-push-sub-issue-422
**コミット:** f9dd294 fix: 422 catch を RequestError.status チェックに変更し dead code を解消する (#2695)
**ファイル:** 2 ファイル変更
**プッシュ:** yes
