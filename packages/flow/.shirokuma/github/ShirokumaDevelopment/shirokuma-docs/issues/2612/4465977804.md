---
number: 2612
database_id: 4465977804
updated_at: "2026-05-16T06:19:56Z"
cached_at: "2026-05-16T07:13:18.164Z"
---

## 作業完了サマリー

`finalize-worker` SubAgent を新設し、`finalize-changes` スキルの `/simplify` と `reviewing-security` 呼び出しを SubAgent コンテキストに閉じ込めた。これにより `implement-flow` 末尾の中間出力（diff・指摘リスト）がメインコンテキストを圧迫する問題（auto-compression 直前の処理停止）の根本原因を解消する。

**PR:** #2614
**ブランチ:** `feat/2612-finalize-worker-subagent` → `develop`

### 変更ファイル

| ファイル | 変更内容 |
|---------|---------|
| `plugin/shirokuma-skills-ja/agents/finalize-worker.md` | 新設。Step A（skip 判定）→ B（`/simplify`）→ C（`reviewing-security`）を SubAgent コンテキスト内で順次実行 |
| `plugin/shirokuma-skills-en/agents/finalize-worker.md` | 新設（EN 同期） |
| `plugin/shirokuma-skills-ja/skills/finalize-changes/SKILL.md` | `allowed-tools` から `Skill` 除外。4 ステップ → 3 ステップ構成に再整理（Step 1: finalize-worker / Step 2: lint docs / Step 3: 改善コミット） |
| `plugin/shirokuma-skills-en/skills/finalize-changes/SKILL.md` | EN 同期 |
| `plugin/specs/skills/finalize-changes/DESIGN.md` | シーケンス図と設計根拠を SubAgent 委任型に更新 |

### 計画レビュー指摘への対応

| ID | 指摘内容 | 対応 |
|----|---------|------|
| H-1 | `reviewing-security` / `/security-review` の用語不整合 | `reviewing-security` プラグインスキルに統一。AI-consumed asset 5 観点を維持 |
| H-2 | SubAgent 出力フォーマット未整合 | `worker-completion-pattern.md` 準拠の YAML フロントマター（`action: CONTINUE`, `status: SUCCESS`）を定義。失敗時の判定基準を明示 |
| M-1 | エラーハンドリングポリシー未記述 | Step B 失敗・Step C 失敗とも `status: SUCCESS` で吸収、致命的エラーのみ `FAIL` と明文化 |
| M-3 | skip 判定二重化方針 | DESIGN.md に「`reviewing-security` 廃止判断は別 Issue、二重判定は意図的維持」を明記 |
| M-4 | Step 番号リネーム未計画 | finalize-changes SKILL.md を Step 1/2/3 にリナンバリング |

### Simplify レビュー反映

- `finalize-worker` Step A 末尾の「二重判定について」note ブロックを削除し DESIGN.md への参照 1 行に置換（3 箇所重複を解消）
- `finalize-worker` Step C 冒頭の判定テーブル（Step A 結果の言い換え）を 1 行に圧縮
- `finalize-changes/SKILL.md` ステップ 1 に「Step A→B→C を順次実行」の接続文を追加し階層対応を明確化

### 検証

- `shirokuma-flow lint docs` → PASS（docs drift なし）
- セキュリティレビュー → HIGH/MEDIUM 0 件（変更は全て AI-consumed asset の Markdown 文書）

### 残作業（本 PR スコープ外）

- 親 Issue 成果物 5「`implement-flow` / `review-flow` 末尾チェーンが停止せずに完走することを手動検証」は、本 PR マージ後にプラグインがリリースされてから実機検証が必要（フォローアップ Issue 化を検討）
- `reviewing-security` プラグインスキル本体の廃止判断は別途派生 Issue を作成（M-3 関連）