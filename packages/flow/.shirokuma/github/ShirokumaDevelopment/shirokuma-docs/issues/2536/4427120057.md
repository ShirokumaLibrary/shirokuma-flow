## 実装完了

ADR-v3-022 第二改訂版に基づく **`migrate-v3-018` CLI 完全削除 + 破壊的変更リリースノート整備** を完了しました。

**PR:** #2541 (base: `epic/2529-6-status-model`)
**ブランチ:** `chore/2536-migrate-cleanup`
**ステータス:** Review

### コミット履歴

| Hash | 内容 |
|------|------|
| `bcacd08` | chore: migrate-v3-018 CLI 削除 + CHANGELOG 破壊的変更整備 (sub6) |
| `09a2d79` | docs: PR #2541 simplify レビュー指摘の修正 - 削除済み migrate-v3-018 docs 参照消去 |

### 達成した受け入れ基準 (AC9)

- [x] `packages/flow/src/commands/items/integrity/migrate-v3-018.ts` 削除（538 行）
- [x] `packages/flow/__tests__/commands/items/integrity-migrate-v3-018.test.ts` 削除（633 行）
- [x] `packages/flow/src/index.ts` から `--migrate-v3-018` / `--dry-run` / `--rollback`（migrate 用）フラグ削除
- [x] `packages/flow/src/commands/items/integrity/index.ts` のコメント参照更新
- [x] `packages/flow/src/commands/items/integrity/classify.ts` JSDoc を ADR-v3-022 準拠に更新
- [x] `packages/flow/CHANGELOG.md` 新規作成（破壊的変更 6 項目 + 過渡期手順 + Added セクション）
- [x] docs から削除済み CLI 参照を消去（troubleshooting.md, items-transition.md）

### CHANGELOG 破壊的変更 6 項目

1. **`approve` の意味変更**: `Review → ToDo` → `Review → Done`。`syncParentStatus` で親 `Backlog → ToDo` 自動同期
2. **`--rollback` フラグ必須化**: ロールバック遷移は明示フラグ必須
3. **`validateStatusTransition` シグネチャ変更**: `validateTransition(itemType, from, to, opts)` に切り替え
4. **`STATUS_TRANSITIONS` 定数廃止**: 4 テーブル分離
5. **`INITIAL_STATUSES` 変更**: `In progress` → `Backlog`
6. **`migrate-v3-018` CLI 削除**: 既存 Issue の自動移行は提供しない

過渡期の対応手順を含む。

### simplify レビュー対応

PR #2541 作成後の単一エージェント simplify レビューで HIGH 級指摘を即時対応:

- **HIGH-1**: `docs/guide/troubleshooting.md` の `--migrate-v3-018` 案内 → 削除し、ADR-v3-022 対応手順に置換
- **HIGH-2**: `docs/specs/cli/items-transition.md` の `--migrate-v3-018` 参照 → 「廃止された (ADR-v3-022)」明記に更新

### セキュリティレビュー

`/security-review` 実行: **No vulnerabilities identified.**

YAML パース、CLI 入力バリデーション (`isIssueNumber`)、GraphQL parameterized variables、tmp file の予測可能だがシングルユーザー scope と既存セキュリティ姿勢を維持。

### テスト結果

- ビルド・型チェック: 通過
- テスト: 159 スイート, 2180 通過

### エピック完了状況

このマージで ADR-v3-022 実装エピック (#2529) のサブ Issue 完了:

| Sub | PR | 状態 |
|-----|----|------|
| sub1 (#2531) | #2537 | マージ済み |
| sub2 (#2532) | #2538 | マージ済み |
| sub3 (#2533) | #2539 | マージ済み |
| sub4 (#2534) | — | テスト全面更新（後続別 PR を予定） |
| sub5 (#2535) | #2540 | レビュー中 |
| sub6 (本 Issue) | #2541 | レビュー中 |

> **注意**: sub4 (#2534) はテスト全面更新（4 テーブル × 6 値 × Forward/Rollback の網羅）で、本 epic のスコープ外として後続 PR を予定。`.legacy.ts.disabled` ファイル群の 6 値版書き直しを含む。

PR #2540 (sub5) と本 PR #2541 (sub6) の両方がマージされれば、ADR-v3-022 実装エピックは sub4 を残してほぼ完成です。
