## 作業完了

親 Issue が close または cancel されたときに、配下の実作業サブ Issue（計画/設計 Issue 以外の Open 子）を自動 close する逆方向連鎖を実装した。`rollback --action cancel` 経路にも `syncChildCloseOnParentClose` の明示フックを追加し、`pr merge` 経由の兄弟子 cascade は本 Issue のスコープ外と整理した。

**ブランチ:** `feat/2502-parent-close-cascade-children`
**PR:** #2548
**計画 Issue:** #2547

### 変更ファイル

| ファイル | 変更内容 |
|---------|---------|
| `packages/flow/src/utils/parent-status.ts` | `cascadeCloseTargets` フィルタを `!isDesignIssue(n)` に簡素化。設計 Issue 以外の Open 子（計画 Issue・実作業サブ Issue を含む）を連動 Close 対象に拡張 |
| `packages/flow/src/commands/issue/rollback/index.ts` | `actionCancel` の `closeIssueById` 直前に `syncChildCloseOnParentClose(..., "NOT_PLANNED")` を追加（方針 B） |
| `packages/flow/src/commands/pr/merge.ts` | `syncChildCloseOnParentClose` 呼び出し（コメントなし、挙動変更なし） |
| `__tests__/utils/parent-status-child-close.test.ts` | 実作業サブ Issue 連動 close、孫非到達、空連鎖シナリオを追加 |
| `__tests__/commands/issue/rollback-cancel-unparent.test.ts` | rollback cancel 経路の連動 close 検証シナリオを追加 |

### 受け入れ基準の達成

- [x] 親 close 時に「実作業サブ Issue」が自動 close される
- [x] PR/孫 Issue を持つ子はエラーにならず適切に扱われる（PR は対象外、孫は再帰なしで非到達）
- [x] 既存の正方向連鎖テストが PASS
- [x] 新規シナリオの単体テスト追加

### 後処理

- `/simplify`: 冗長な二重述語の簡素化、コメント整理（`#2502` 参照・装飾区切り・WHAT 説明の除去）
- `reviewing-security`: 脆弱性なし

### コミット

| Commit | 内容 |
|--------|------|
| `567c341` | feat(workflow): 親 close 時に実作業サブ Issue を自動 close |
| `9b7c554` | refactor: simplify による改善 — コメント整理・フィルタ簡素化 |
| `e55e6a2f` | test: @testdoc コメントを追加 — pre-push フック対応 |

### Status 注記

6 値モデル（ADR-v3-022）下で Issue は `In progress → Review` 遷移を持たないため、本 Issue は In progress のまま PR マージ時に Done に遷移する想定。
