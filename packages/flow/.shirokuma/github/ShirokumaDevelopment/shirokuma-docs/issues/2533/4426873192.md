## 実装完了

ADR-v3-022 第二改訂版に基づく **parent-status 6値対応 + integrity Approved → Done 統一 + 旧 STATUS_TRANSITIONS / validateStatusTransition 削除 + 全呼び出し側の新 API 移行** を実装しました。PR #2537 (sub1) のレビューで sub3 スコープに追加された Critical/High 指摘を全件解消。

**PR:** #2539 (base: `epic/2529-6-status-model`)
**ブランチ:** `feat/2533-parent-status-integrity`
**ステータス:** Review

### コミット履歴

| Hash | 内容 |
|------|------|
| `7194d525` | feat: parent-status 6値対応 + integrity Approved→Done + 旧 API 削除 (sub3) |
| `7f0de4a` | refactor: PR #2539 simplify レビュー指摘の修正 |

### 受け入れ基準（AC8）達成

- [x] **AC8**: `deriveExpectedParentStatus` が 6 値モデルで正しく親 Status を派生
  - 全サブ Backlog → 親 Backlog（`isInvestigationPending`）
  - 全サブ ToDo → 親 ToDo（`isReadyForImplementation`）
  - Backlog/ToDo 混在 → 親 Backlog（保守的）

### PR #2537 レビュー追加スコープ達成

- [x] **Critical**: `Approved` LEGACY 三者不整合解消（`integrity/index.ts` の `--fix` を Approved → Done に統一、`fixes.action` を `"approved-to-done"` にリネーム、ログメッセージ `Migrated #X status: Approved → Done` に更新）
- [x] **High**: `pr/merge.ts:425` の `isBacklogEquivalent` を `isReadyForImplementation` に置換（メッセージ「次の計画 #N が ToDo です（着手準備完了）」に修正）
- [x] **High**: `parent-status.ts:389` の `isBacklogEquivalent` 使用箇所の意味変更対応（全サブ Backlog → 親 Backlog 導出ロジック追加）
- [x] **High**: 旧 `STATUS_TRANSITIONS[Review]` から `ToDo` 削除（旧テーブル全体を削除）
- [x] **関連**: 全 7 箇所の呼び出し側を新 `validateTransition(itemType, ...)` に移行
  - `commands/issue/close/index.ts`, `update/index.ts`, `reopen/index.ts`
  - `commands/status/transition/index.ts`, `allowed.ts`, `get.ts`
  - `utils/project-fields.ts`

### 旧 API 削除完了

- [x] `STATUS_TRANSITIONS` 定数を `status-workflow.ts` から削除
- [x] `validateStatusTransition(from, to)` 関数を `status-workflow.ts` から削除
- [x] @deprecated 解除（実体ごと削除）

### simplify レビュー対応

3 エージェント並列レビュー（コード再利用・コード品質・効率）の High/Low 級指摘を即時対応:

- `integrity/index.ts`: `"Approved"` 文字列リテラル → `LEGACY_STATUS_VALUES.APPROVED_LEGACY`
- `issue/reopen/index.ts`: `"ToDo"` 文字列リテラル 3 箇所 → `STATUS_VALUES.TODO`
- `begin.ts`: dead reference コメント `validateStatusTransition` → `validateTransition`
- `parent-status.ts`: 未使用 `isBlockedEquivalent` import 削除
- `status/allowed.ts`: 静的照会で rollback を出力しない理由のコメント追加

未対応（スコープ外として記録）:
- **Medium**: `INCOMPLETE_SUB_ISSUE_STATUSES` の LEGACY 値漏れ (`ON_HOLD_LEGACY`, `PENDING_LEGACY`, `READY_LEGACY`)。既存の不整合で、本 sub3 のスコープ外。フォローアップ Issue 候補。
- **Medium**: `deriveExpectedParentStatus` Backlog/ToDo 混在 → Backlog 保守的の挙動。設計判断のため、計画書/ADR の補足で対応する方針。

### セキュリティレビュー

`/security-review` 実行: **No HIGH or MEDIUM security findings.**

GraphQL は parameterized variables、新 log statements は status name と Issue number のみ、`--force` は意図的なエスケープハッチで認証境界ではない。

### テスト結果

- ビルド・型チェック: 通過
- テスト: 160 suites passed, 2195 tests passed, 2 skipped

既存テストの最小限の更新（`STATUS_TRANSITIONS` モック → `getAllowedTransitions` モック、6 値モデル期待値の修正）で全件通過。

### 後続作業

- sub4 (#2534): テスト全面更新（4 テーブル × 6 値 × Forward/Rollback 網羅 + `.legacy.ts.disabled` ファイル群の 6 値版書き直し）
- sub5 (#2535): プラグイン (prepare-flow / design-flow から Review 遷移削除) + docs + Projects
- sub6 (#2536): migrate-v3-018 削除 + --rollback 破壊的変更リリースノート
