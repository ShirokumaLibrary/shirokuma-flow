## 実装完了

ADR-v3-022 第二改訂版に基づく **動的ルーティング + approve 意味変更 + --rollback フラグ必須化** を 4 コマンドに導入しました。

**PR:** #2538 (base: `epic/2529-6-status-model`)
**ブランチ:** `feat/2532-dynamic-routing`
**ステータス:** Review

### コミット履歴

| Hash | 内容 |
|------|------|
| `74ed58c` | feat: 動的ルーティング + approve 意味変更 + --rollback 必須化を4コマンドに導入 |
| `17407ca` | refactor: simplify レビュー指摘の修正 + 旧テストファイル削除クリーンアップ |

### 受け入れ基準（AC5, AC6, AC7）達成

- [x] **AC5**: `submit` / `reject` が `itemType` で動的ルーティング
- [x] **AC6**: `approve` が `Review → Done` に変更、親 Issue を `Backlog → ToDo` 自動同期
- [x] **AC7**: `--rollback` フラグなしで rollback 遷移先指定はエラー
- [x] `status transition` が `itemType` を自動判定（`resolveCurrentStatus()` ヘルパー再利用）

### 変更ファイル

| ファイル | 内容 |
|---------|------|
| `src/commands/status/transition/index.ts` | `--rollback` フラグ追加、`validateTransition` で遷移検証、`kind`/`item_type` を出力に追加 |
| `src/commands/status/index.ts` | transition コマンドオプション定義 + approve 説明文を `Review → Done` に更新 |
| `src/commands/submit.ts` | 動的ルーティング (issue: Backlog→Review, pr: In progress→Review)、`resolveCurrentStatus` ヘルパー再利用 |
| `src/commands/status/approve/index.ts` | **意味変更** Review → Done、親 Issue を `Backlog → ToDo` 自動同期 |
| `src/commands/reject.ts` | `--rollback` 必須化、itemType 別遷移先 (issue → Backlog, pr → In progress)、`resolveCurrentStatus` ヘルパー再利用 |

### simplify レビュー対応

PR #2538 作成後の 3 エージェント並列レビュー（コード再利用・コード品質・効率）で High 級指摘を即時対応:

- PR/Issue itemType 検出を 3 ファイル（submit/reject/transition）でインライン実装していたものを、既存の `resolveCurrentStatus()` ヘルパーに統一（重複解消 + 二重 GraphQL 呼び出し解消）
- approve.ts の dead `syncedParentToTodo` フラグと冗長な `validateTransition` precheck を削除
- 未使用の `--from` フラグを削除
- narration コメントを整理

### セキュリティレビュー

`/security-review` 実行: **No HIGH or MEDIUM security findings.**

GraphQL 呼び出しはパラメータ化済み、`isIssueNumber` バリデーションでパストラバーサル防止、`options.reason` はファイル本文として書き込み（コマンドインジェクション対象外）。

### テスト結果

- ビルド・型チェック: 通過
- テスト: 160 suites passed, 2195 tests passed, 2 skipped
- 4 テストファイルを `.legacy.ts.disabled` にリネーム（sub4 で 6 値モデル版書き直し予定）:
  - `approve.test.ts` (Review → ToDo 前提)
  - `reject.test.ts` (--rollback 未指定・旧遷移先前提)
  - `prepare-to-implement.test.ts` (旧 5 値モデル遷移経路前提)
  - `reject-cycle.test.ts` (--rollback 未指定・旧遷移先前提)

### 後続作業

- sub3 (#2533): parent-status 6 値対応 + integrity の Backlog 認識 + 既存呼び出し側を新 API に移行 + 旧 API 削除
- sub4 (#2534): テスト全面更新（.legacy.ts.disabled の 6 値版書き直し含む）
- sub5 (#2535): プラグイン (prepare-flow / design-flow から Review 遷移削除) + docs + Projects
- sub6 (#2536): migrate-v3-018 削除 + --rollback 破壊的変更リリースノート
