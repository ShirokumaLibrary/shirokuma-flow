## レビュー対応完了

レビュー結果（FAIL: Critical 1, High 3, Medium 4, Low 3）に対する対応をまとめます。

### マージ前ブロック対応（コミット `af04e69e`）

| 指摘 | 対応 |
|------|------|
| `classify.ts:558` description が "5 値モデル (ADR-v3-018)" のまま | "6 値モデル (ADR-v3-022 第二改訂版)" に修正 + sub3 で再整合される旨を追記 |
| `issue/add/index.ts:162` コメント "Backlog / Pending のみ許可" | "Backlog のみ許可、ADR-v3-022 第二改訂版 #2531" に修正 |

ビルド・全テスト通過（164 suites, 2225 passed, 2 skipped）。

### sub3 (#2533) スコープに追加（コメントで明示済み）

| 指摘 | 担当 sub | 対応方針 |
|------|---------|---------|
| **[Critical]** `Approved` LEGACY 三者不整合（`normalizeLegacyStatus` → Done と `--fix` → Review の食い違い） | sub3 | `integrity/index.ts:874-891` の `--fix` 動作を `Approved → Done` に変更、`fixes.action` を `"approved-to-done"` にリネーム、`logger.success` メッセージも修正 |
| **[High]** `pr/merge.ts:425` の `isBacklogEquivalent` 使用箇所の意味ずれ | sub3 | 「次の計画」フィルタを `isReadyForImplementation` (ToDo 専用) に置換 |
| **[High]** `parent-status.ts:389` の `isBacklogEquivalent` 使用箇所の意味変更 | sub3 | 「全サブ Backlog → 親 Backlog 導出」分岐を `isInvestigationPending` で追加 |
| **[High]** 旧 `STATUS_TRANSITIONS[Review]` の `ToDo` 残置 | sub3 | 旧 API 削除時 (sub3 末尾) に新仕様準拠で `Review → ToDo` を削除 |

### sub4 (#2534) スコープに追加（コメントで明示済み）

| 指摘 | 担当 sub | 対応方針 |
|------|---------|---------|
| **[Medium]** `add.legacy.ts.disabled` の status 非依存 28 テストのカバレッジ低下 | sub4 | sub4 着手時の最優先タスクとして、status 検証 5 テストのみ disable に分離し、cmdAddComment / cmdAddDiscussion / labelRules / IssueType / cache 系の 28 テストを即時復活 |
| **[High]** `INCOMPLETE_SUB_ISSUE_STATUSES` の `APPROVED_LEGACY` 残置の論理矛盾 | sub3 (関連) / sub4 (テスト) | sub3 で `APPROVED_LEGACY` を削除する戦略を sub3 / sub6 で議論。本 PR ではコメント残置のまま |

### 取り下げ・反映なし

- **[Medium]** `gh-config.ts:62-63` のコメント補足 → 「問題なし。取り下げ」とレビュアー自身が判断
- **[Medium]** `STATUS_ORDER` の意味補足 → 後続 sub5 (docs 更新) で `cli-status-update-policy.md` に明記する方針として吸収
- **[Low]** `LEGACY_STATUS_VALUES.NOT_PLANNED` の値が "Cancelled" → 6 値モデル整理の本 PR スコープ外、フォローアップ Issue 候補
- **[Low]** `LEGACY_STATUS_VALUES` 73-78 行のコメント簡潔すぎ → sub5 (docs 更新) で吸収
- **[Low]** `gh-config.test.ts:171` の `#2439` コメント残置 → レビュアー自身が「更新済み・問題なし」と判定

### メモリ昇格候補

レビュアー提案の以下 2 メモリは `legacy-status-migration-checklist.md` / `equivalent-helper-semantic-shift.md` として記録され、再発時に `packages/flow/.shirokuma/rules/shirokuma-flow/` 配下のルールへ昇格を検討する旨を Evolution シグナルとして残します。

- LEGACY 値の意味変更時は `normalizeLegacyStatus` / `--fix action` / `description` / `metadata` の 4 点を同時に更新する
- `is*Equivalent` ヘルパーの判定範囲拡張時は呼び出し側の意味付けを全件確認する

### 追加修正コミット

- `af04e69e`: fix(lifecycle): PR #2537 レビュー指摘のマージ前ブロック対応 (#2531)

レビュアーへの依頼: マージ可否判定の再確認をお願いします。Critical 1 と High 3 は本 PR スコープ外として sub3 で対応します（破壊的変更を含むため、独立 PR で履歴を残す方が安全）。
