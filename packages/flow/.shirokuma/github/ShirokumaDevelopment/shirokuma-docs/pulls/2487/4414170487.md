## レビュー対応完了 (再レビュー Round 2)

再レビュー（PASS、Low 2 件）の推奨事項に対応した。L-2 を Logger 注入パターンで実装し、L-1 の仕様書統一も同時に反映。simplify レビューで既存 `createMockLogger` ヘルパの再利用も改善した。

| 指摘 | 対応 | コミット |
|------|------|---------|
| L-1: `items-create.md` で stdin 表記が `discussion add` のみ | `issue add` / `issue comment` セクションにも「`-` を指定して stdin から読み込む」を追記 | `1134c11` |
| L-2: `readFileWithFrontmatter` が直接 `console.error` を呼ぶ（Logger 統合の余地） | シグネチャを `(source, logger)` に変更し `logger.error(...)` 経由で出力するよう変更。呼び出し元 3 箇所（`items/add/discussion.ts` / `issue/add/index.ts` / `issue/comment/index.ts`）と既存テストも追従 | `1134c11` |
| simplify (reuse): inline `createMockLogger` factory が既存ヘルパと重複 | `__tests__/helpers/command-test-utils.ts` の共通 `createMockLogger` を import するよう置換（戻り値型も `Logger` で統一） | `4c1b56c` |

## 検証結果

| チェック | 結果 |
|---------|------|
| `pnpm --filter @shirokuma-library/shirokuma-flow build` | PASS |
| `pnpm --filter @shirokuma-library/shirokuma-flow test` | 170 suites / 2407 tests passing |
| `shared.test.ts` (改善後) | 6/6 PASS |
| `shirokuma-flow lint annotations` | 0 errors |
| `shirokuma-flow lint docs` | 0 errors |
| `/security-review` | No new vulnerabilities |
