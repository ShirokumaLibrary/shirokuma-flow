## レビュー対応完了

PASS 判定後の推奨事項（Medium 2 件 + Low 1 件）に対応した。Low 2 件（L-1: `requirements-worker.md` の `--category Ideas` 無効、L-2: `issue add`/`issue comment` の `moveFile` での `-` 非対応）は pre-existing で本 PR スコープ外のため、別 Issue 化推奨とした。

| 指摘 | 対応 | コミット |
|------|------|---------|
| M-1: `readFileWithFrontmatter` の stdin 経路の単体テスト不在 | 新規 `packages/flow/__tests__/commands/items/add/shared.test.ts`（6 テスト：stdin frontmatter / stdin no-frontmatter / readBodyFile throw / 通常ファイル / null / body フィールド除外） | `8b1db7b0` |
| M-2: stdin TTY 時のエラーメッセージが不親切 | `shared.ts` の `catch` で `console.error(e instanceof Error ? e.message : String(e))` を追加し、`readBodyFile` の `stdin is a TTY (no piped input)` ヒントをユーザーに届くように変更 | `8b1db7b0` |
| L-3: `items-create.md` で stdin 未明記 | `discussion add <file>` セクションに「`-` を指定して stdin から読み込む」を追記 | `8b1db7b0` |
| simplify: テストの narrating コメント整理 | `(#2487 PR レビュー M-1)` / `[#2487 PR レビュー M-1]` を `(#2487)` / `[#2487]` に短縮、`（M-2）` 参照を削除 | `3c9201b` |

## スコープ外（別 Issue 化推奨）

- **L-1**: `plugin/shirokuma-skills-{en,ja}/agents/requirements-worker.md` の `--category Ideas` フラグ無効。frontmatter に移すべき。
- **L-2**: `issue add` / `issue comment` の `moveFile` で `file !== "-"` ガード不在。`discussion add` と同じパターンに統一すべき。

## 検証結果

| チェック | 結果 |
|---------|------|
| `pnpm --filter @shirokuma-library/shirokuma-flow build` | PASS |
| `pnpm --filter @shirokuma-library/shirokuma-flow test` | 170 suites / 2407 tests passing |
| 新規 `shared.test.ts` | 6/6 PASS |
| `shirokuma-flow lint annotations` | 0 errors |
| `shirokuma-flow lint docs` | 0 errors |
| `/security-review` | No new vulnerabilities |
